<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CivicLedger - Allocate Funds</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <script src="https://kit.fontawesome.com/a076d05399.js" crossorigin="anonymous"></script>
    <style>
        body {
            background-color: #f8f9fa;
        }
        .container {
            background: #ffffff;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.1);
            margin-top: 30px;
        }
        .btn-primary {
            transition: 0.3s;
        }
        .btn-primary:hover {
            background-color: #0056b3;
        }
        .navbar {
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.1);
        }
    </style>
</head>
<body>
    <script>
        if(<%= isAdded %>) {
            alert("Transaction successful");
        }
    </script>

    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="/indexSchool"><i class="fas fa-university"></i> CivicLedger</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item"><a class="nav-link" href="/indexSchool">Home</a></li>
                    <li class="nav-item"><a class="nav-link active" href="/addSchool">Allocate Funds</a></li>
                    <li class="nav-item"><a class="nav-link" href="/graph">Fund Tracking</a></li>
                    <li class="nav-item"><a class="nav-link" href="/department_transactions">Transactions</a></li>
                </ul>
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item"><a class="nav-link">Hello, <%= username %></a></li>
                    <li class="nav-item">
                        <form action="/logout?_method=DELETE" method="POST">
                            <button class="btn btn-outline-danger" type="submit">Logout</button>
                        </form>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container">
        <h2 class="text-center mb-3">Allocation & Tracking of Public Funds</h2>
        <p class="text-center text-muted">User Type: District Government</p>
        <p class="text-center fw-bold">Balance: Rs. <%= districtBalance %></p>
        
        <h4 class="mb-4">Allocate Funds to School</h4>
        <form id="gtogForm" method="POST" action="/allocate/dtos">
            <div class="mb-3">
                <label class="form-label">Your Name</label>
                <input type="text" class="form-control" name="name1" value="<%= username %>" readonly>
            </div>
            <div class="mb-3">
                <label class="form-label">Amount (Rs.)</label>
                <input type="number" class="form-control" name="amount" max="<%= districtBalance %>" placeholder="Enter amount" required>
            </div>
            <div class="mb-3">
                <label class="form-label">Select Project</label>
                <select name="project" id="project" class="form-select" required>
                    <% projects.forEach(function(project){ %>
                        <option value="<%= project.name %>"><%= project.name %></option>
                    <% }) %>
                </select>
            </div>
            <div class="mb-3">
                <label class="form-label">Select School</label>
                <select name="name2" id="name2" class="form-select" required>
                    <% schools.forEach(function(school){ %>
                        <option value="<%= school.name %>"><%= school.name %></option>
                    <% }) %>
                </select>
            </div>
            <div class="text-center">
                <button class="btn btn-primary w-50" type="submit">Submit</button>
            </div>
        </form>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
